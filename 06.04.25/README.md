<!-- O projeto foi criado em Java, utilizando Maven(usado uma extensão do VS CODE), criando 2 classes main(main e veiculo.java) e uma de teste, para fazer a verificação e ver se está rodando normalmente, e que retorne conforme o requisitado ( o teste unitário). Em main é feito o sistema que o usuário irá interagir e fazer os IFs(no qual estão presentes na classe veiculo.java), na qual após o input do usuário ele irá retornar os valores, caso haja multa ou não por atraso, isso tudo com as escolhas feitas pelo sistema "frente" (main) e condições de veiculo.java. 
A pasta target foi criada automaticamente pelo Maven, na qual contém o conteúdo feito na principal (src).
O pom.xml ele é a função principal do Maven o do JUnit, onde fica localizada as dependências, versões, etc.  -->